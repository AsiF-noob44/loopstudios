/** @type {import('tailwindcss').Config} */
module.exports = {
  // In Tailwind v4, most configuration is done via @theme in CSS
  // This file is kept minimal for any plugins or special configurations
  plugins: [],
};
